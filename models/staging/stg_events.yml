version: 2

models:
  - name: stg_events
    columns:
      - name: event_date
        tests:
          - not_null
          - unique
      - name: user_pseudo_id
        tests:
          - not_null
      - name: event_name
        tests:
          - not_null
